#!/usr/bin/env bash
# This script displays the PID along with the process  name, of process whose name contain the word bash

script_pid=$$

processes=$(ps -eo pid,cmd | awk -v script_pid="$script_pid" '/bash/ && !/awk/ {print $1, $2}')

echo "$processes"
